{# Mounts Video.js (fetched on demand from the configured URLs) on every `[data-setono-sylius-video-player]`; prepended to `sylius.shop.layout.javascripts` when the Cloudflare Stream type is enabled. #}
<script src="{{ asset('bundles/setonosyliusvideoplugin/setono-sylius-video-plugin-shop.js') }}" data-video-js-script="{{ video_js_script }}" data-video-js-stylesheet="{{ video_js_stylesheet }}" defer></script>
