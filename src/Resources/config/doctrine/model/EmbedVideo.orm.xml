<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!-- html is nullable at the DB level because STI rows of other types do not have embed
         markup; the application-layer validator enforces NotBlank for embed videos. -->
    <mapped-superclass name="Setono\SyliusVideoPlugin\Model\EmbedVideo">
        <field name="html" type="text" column="html" nullable="true"/>
    </mapped-superclass>

</doctrine-mapping>
