<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!-- url is nullable at the DB level because STI rows of other types do not have a url; the
         application-layer validator enforces NotBlank + Url for url videos. -->
    <mapped-superclass name="Setono\SyliusVideoPlugin\Model\UrlProductVideo">
        <field name="url" type="string" length="2048" column="url" nullable="true"/>
    </mapped-superclass>

</doctrine-mapping>
