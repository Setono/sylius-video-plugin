imports:
    - { resource: "@SyliusCoreBundle/Resources/config/app/config.yml" }
    - { resource: "@SyliusAdminBundle/Resources/config/app/config.yml" }
    - { resource: "@SyliusShopBundle/Resources/config/app/config.yml" }
    - { resource: "@SyliusApiBundle/Resources/config/app/config.yaml" }

parameters:
    sylius_core.public_dir: '%kernel.project_dir%/public'

sylius_shop:
    product_grid:
        include_all_descendants: true

sylius_api:
    enabled: true

# Overrides the product model so it owns the inverse ProductVideo association — exactly the step
# the README asks adopting applications to perform.
sylius_product:
    resources:
        product:
            classes:
                model: Setono\SyliusVideoPlugin\Tests\Application\Entity\Product

# The README's worked example of an application-defined video type (see tests/Application/Entity/Video).
# Registering the model as a resource is all the plugin needs to add it to the STI map and the type selector.
sylius_resource:
    resources:
        app.youtube_video:
            classes:
                model: Setono\SyliusVideoPlugin\Tests\Application\Entity\Video\YoutubeProductVideo
